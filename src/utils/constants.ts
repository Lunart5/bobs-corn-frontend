export const field_required_text = "Field is required";
export const invalid_email = "Mail is not valid";
export const loading_text = "Loading...";

export const email_regex = /^[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}$/i;

export const field_min_text = (value: number) =>
  `Minimum ${value} characters required`;

export const field_max_text = (value: number) =>
  `Maximun ${value} characters`;
